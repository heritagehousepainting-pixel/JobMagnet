# JobMagnet — runtime dependencies (kept deliberately light, like FirstBack).
psycopg[binary]>=3.2
Flask>=3.0
gunicorn>=21.2          # production WSGI server (Render); the dev server is local-only
requests>=2.31
# Optional: only needed when FIRSTBACK/JOBMAGNET provider is "claude".
# anthropic>=0.40
# Optional: only needed once Stripe billing is configured (STRIPE_SECRET_KEY set).
# stripe>=9.0
