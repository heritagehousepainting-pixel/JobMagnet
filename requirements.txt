# JobMagnet — runtime dependencies (kept deliberately light, like FirstBack).
psycopg[binary]>=3.2
Flask>=3.0
gunicorn>=21.2          # production WSGI server (Render); the dev server is local-only
requests>=2.31
anthropic>=0.40         # used when JOBMAGNET_PROVIDER=claude
stripe>=9.0             # used when Stripe billing env vars are configured
